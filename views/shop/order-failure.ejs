<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Order Failed</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="icon" href="/public/img/core-img/favicon.ico" />
  <link rel="stylesheet" href="/public/css/core-style.css" />
  <link rel="stylesheet" href="/public/css/style.css" />
  <style>
    :root{--danger:#e03131;--ink:#212529;--muted:#868e96;--bg:#fff5f5;--brand:#b197fc}
    body{background:#fff}
    .topbar{padding:14px 22px;border-bottom:1px solid #f1f3f5;display:flex;align-items:center;justify-content:space-between}
    .topbar a{color:var(--ink);text-decoration:none}
    .wrap{max-width:760px;margin:40px auto;padding:0 16px}
    .panel{border:1px solid #ffe3e3;background:var(--bg);border-radius:14px;box-shadow:0 10px 30px rgba(0,0,0,.04)}
    .panel .hd{padding:18px 18px 0}
    .panel .bd{padding:18px}
    .pill{display:inline-flex;align-items:center;gap:8px;background:#fff;border:1px solid #ffe3e3;color:var(--danger);padding:6px 10px;border-radius:999px;font-weight:700}
    .x{width:10px;height:10px;border-radius:50%;background:var(--danger);display:inline-block}
    .msg{border:1px dashed #ffc9c9;background:#fff;border-radius:12px;padding:12px;margin-top:10px}
    .btn{display:inline-flex;align-items:center;gap:8px;border-radius:999px;padding:10px 14px;font-weight:600;border:1px solid transparent;cursor:pointer;text-decoration:none}
    .btn-primary{background:var(--brand);color:#fff}
    .btn-ghost{background:#fff;border-color:#dee2e6;color:#343a40}
    .tips{color:var(--muted);font-size:14px}
    .list{margin:10px 0 0 16px}
  </style>
</head>
<body>
  <div class="topbar">
    <a href="/"><img src="/public/img/core-img/logo.png" style="height:34px" alt="Logo"></a>
    <nav>
      <a href="/shop">Shop</a> · <a href="/cart">Cart</a> · <strong>Checkout</strong>
    </nav>
  </div>

  <div class="wrap">
    <div class="panel">
      <div class="hd">
        <span class="pill"><span class="x"></span> Payment Failed</span>
      </div>
      <div class="bd">
        <h2 class="mb-2">We couldn’t complete your payment</h2>
        <p class="tips">Order ID: <strong>#<%= orderId || (order && (order.orderId || order._id)) || '—' %></strong></p>

        <div class="msg">
          <strong>What happened?</strong>
          <p style="margin:.3rem 0 0"><%= message || 'Your transaction was not successful. No money has been captured.' %></p>
        </div>

        <div class="list">
          <ul>
            <li>Make sure your internet connection is stable.</li>
            <li>Use a valid card / UPI with sufficient funds and completed KYC.</li>
            <li>If money was debited, it is usually auto-reversed by your bank within a few minutes.</li>
          </ul>
        </div>

        <div class="mt-3" style="display:flex;gap:10px;flex-wrap:wrap">
          <% if (typeof canRetry === 'undefined' || canRetry) { %>
            <a class="btn btn-primary" href="<%= retryUrl || ('/checkout/pay/' + (orderId || (order && order._id))) %>">
              <i class="fa fa-redo"></i> Try Again
            </a>
          <% } %>
          <a class="btn btn-ghost" href="/cart"><i class="fa fa-shopping-cart"></i> Back to Cart</a>
          <a class="btn btn-ghost" href="/support"><i class="fa fa-life-ring"></i> Need Help</a>
        </div>
      </div>
    </div>
  </div>

  <!-- (Optional) your theme JS -->
  <script src="/public/js/jquery/jquery-2.2.4.min.js"></script>
  <script src="/public/js/bootstrap.min.js"></script>
</body>
</html>

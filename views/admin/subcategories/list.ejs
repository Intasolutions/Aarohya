<%- include('../../partials/admin_head', { pageTitle: 'All Subcategories' }) %>

<div class="card shadow-lg p-4 rounded-4 border-0" style="background: #fff;">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold text-dark m-0">All Subcategories</h2>
    <!-- <a href="/admin/subcategories/add" class="btn btn-accent px-4 rounded-pill">
      <i class="bi bi-plus-lg me-1"></i> Add Subcategory
    </a> -->
  </div>

  <div class="table-responsive">
    <table class="table align-middle">
      <thead class="table-light border-bottom">
        <tr class="text-uppercase small fw-bold text-secondary">
          <th>#</th>
          <th>Name</th>
          <th>Slug</th>
          <th>Category</th>
          <th>Blocked</th>
          <th class="text-center">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% subcategories.forEach((sc, index) => { %>
          <tr class="table-row-hover">
            <td class="fw-semibold text-muted"><%= index + 1 %></td>
            <td class="fw-semibold text-dark"><%= sc.name %></td>
            <td><span class="badge rounded-pill bg-light text-dark"><%= sc.slug %></span></td>
            <td><span class="badge rounded-pill bg-accent-subtle text-accent"><%= sc.category ? sc.category.name : 'N/A' %></span></td>
            <td>
              <% if (sc.isBlocked) { %>
                <span class="badge bg-danger">Blocked</span>
              <% } else { %>
                <span class="badge bg-success">Active</span>
              <% } %>
            </td>
            <td class="text-center">
              <a href="/admin/subcategories/edit/<%= sc._id %>" class="btn btn-sm btn-outline-accent rounded-pill px-3 me-1">
                <i class="bi bi-pencil-square"></i>
              </a>
              <form action="/admin/subcategories/delete/<%= sc._id %>" method="POST" class="d-inline">
                <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill px-3"
                  onclick="return confirm('Are you sure?')">
                  <i class="bi bi-trash"></i>
                </button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<%- include('../../partials/admin_footer') %>